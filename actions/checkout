jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # This step is crucial! It checks out your code.
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install
